Теперь запрос будет несколько для нескольких списов таблиц.

Необходимо обернуть теукщий модуль в метод, который можно вызывать несколько раз.

Модуль должен принимать на вход:
1) Путь к csv со списом таблиц
2) 